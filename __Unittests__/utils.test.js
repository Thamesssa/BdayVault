import { userInfo, User } from "../js/utils";

// const userInfo = require("../js/utils")

describe('[userInfo]', () => {
    test('[userInfo] is defined ', () => {
        expect(userInfo).toBeDefined()
        
    });
    test('[userInfo] is an array ', () => {

        let newUser = new r
        expect(userInfo.isArray).toBe(true)
        
    });
});